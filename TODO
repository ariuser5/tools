- Create a "push" (real) subscription mode.
- Extract whole GCP into a separate repository
  OR - put all GCP in a folder
- Update logging options to allow flag counter (-v, -vv, -vvv)
- Add functionality to for GmailWatchManager to hijack the existing watch. This will force the watch manager to delete the watch state when stop-watch method is called even if it was not created by it.
 (for testing purposes)